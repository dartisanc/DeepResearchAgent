"""AgentEvaluator Template"""

from src.environment.quickbacktest.base_types import BaseSignalBenchmark
import pandas as pd
import numpy as np
import matplotlib as plt

class AgentSignalEvaluator(BaseSignalBenchmark):
    """
    AgentSignalEvaluator
    ==============

    This class is defined as a template for evaluating the performance of trading signals generated by an agent.

    Data access as initiialized in BaseSignalBenchmark as:
        self.close: pd.DataFrame = self.pivot_frame["close"]
        self.volume: pd.DataFrame = self.pivot_frame["volume"]
        self.open: pd.DataFrame = self.pivot_frame["open"]
        self.high: pd.DataFrame = self.pivot_frame["high"]
        self.low: pd.DataFrame = self.pivot_frame["low"]
        self.amount: pd.DataFrame = self.pivot_frame["amount"]
        self.signal: pd.DataFrame = self.pivot_frame["signal"]
        self.factor1: pd.DataFrame = self.pivot_frame["factor1"]
        self.factor2: pd.DataFrame = self.pivot_frame["factor2"]

    The goal pf this evluator is to study the usefulness of the signal generated by the agent, and thus the signal can be improved later.
    """

    def evaluate(self):
      """
      The method is defined as a placeholder for the evaluation logic.
      It should be used as evaluation of the signal performance, such as correlation, hit_rate or any other metrics.
      The output should in the format of dict, which contains the any evaluation results
      For example:
      {
    "signal": {
        "correlation": 0.5,
        "hit_rate": 0.6,
        "other_metric": 0.7
    },
    "factor1": {
        "correlation": 0.4,
        "hit_rate": 0.5,
        "other_metric": 0.6
    },
    "factor2": {
        "correlation": 0.3,
        "hit_rate": 0.4,
        "other_metric": 0.5
      }
    }
      """
      pass